mment in sq

--Create Database:

CREATE database malledb;

--Create Tables:

CREATE TABLE metatable (
        id INT NOT NULL AUTO_INCREMENT,
        d_key VARCHAR(100) NOT NULL,
        m_count int NOT NULL,
        b_count int NOT NULL,
        t_count int NOT NULL,
        PRIMARY KEY (ID)
        );